<h1>User's Signed Up</h1>

<ul>
	<% @users.each do |user|%>
		<li>
			<%= link_to user.first_name + " " + user.last_name, user_path(user) %>
			<%= user.username %>
			<%= user.email %>

		</li>
	<% end %>
</ul>

<%= link_to 'Home', blogs_path %>
